local config = {
    volume = 75,
    resolucao = {largura = 1920, altura = 1080},
    fullscreen = true
}

print("Volume:", config.volume)
print("Resolução:", config.resolucao.largura .. "x" .. config.resolucao.altura)
print("Fullscreen:", config.fullscreen)